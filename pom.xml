<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>jp.co.ogis-ri.nautible.app</groupId>
  <artifactId>nautible-app-ms-stock-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <properties>
    <compiler-plugin.version>3.8.1</compiler-plugin.version>
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <org.mapstruct.version>1.6.3</org.mapstruct.version>
    <swagger-annotations.version>1.6.15</swagger-annotations.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <quarkus-plugin.version>3.21.4</quarkus-plugin.version>
    <quarkus.platform.artifact-id>quarkus-universe-bom</quarkus.platform.artifact-id>
    <quarkus.platform.group-id>io.quarkus</quarkus.platform.group-id>
    <quarkus.platform.version>3.21.4</quarkus.platform.version>
    <surefire-plugin.version>3.5.3</surefire-plugin.version>
    <jakarta.validation-api.version>3.1.1</jakarta.validation-api.version>
    <jakarta.annotation-api.version>3.0.0</jakarta.annotation-api.version>
    <jakarta.interceptor-api.version>2.2.0</jakarta.interceptor-api.version>
    <jakarta.ws.rs-api.version>4.0.0</jakarta.ws.rs-api.version>
    <jakarta.enterprise.cdi-api.version>4.1.0</jakarta.enterprise.cdi-api.version>
    <openapi.generator.maven.plugin.version>7.13.0</openapi.generator.maven.plugin.version>
    <nautible.app.stock.groupId>jp.co.ogis-ri.nautible.app</nautible.app.stock.groupId>
    <nautible.app.stock.name>nautible-app-ms-stock</nautible.app.stock.name>
    <nautible.app.stock.version>1.0.0-SNAPSHOT</nautible.app.stock.version>
    <dapr.sdk.version>1.14.1</dapr.sdk.version>
    <okhttp.version>4.12.0</okhttp.version>
    <okio.version>3.11.0</okio.version>
    <jandex.maven.plugin.version>1.2.3</jandex.maven.plugin.version>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.dapr</groupId>
        <artifactId>dapr-sdk</artifactId>
        <version>${dapr.sdk.version}</version>
      </dependency>
      <!-- struct -->
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${org.mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct-processor</artifactId>
        <version>${org.mapstruct.version}</version>
        <scope>provided</scope>
      </dependency>
      <!-- https://mvnrepository.com/artifact/io.swagger/swagger-annotations -->
      <dependency>
        <groupId>io.swagger</groupId>
        <artifactId>swagger-annotations</artifactId>
        <version>${swagger-annotations.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.validation</groupId>
        <artifactId>jakarta.validation-api</artifactId>
        <version>${jakarta.validation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.annotation</groupId>
        <artifactId>jakarta.annotation-api</artifactId>
        <version>${jakarta.annotation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.interceptor</groupId>
        <artifactId>jakarta.interceptor-api</artifactId>
        <version>${jakarta.interceptor-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.ws.rs</groupId>
        <artifactId>jakarta.ws.rs-api</artifactId>
        <version>${jakarta.ws.rs-api.version}</version>
      </dependency>
      <!-- quarkusとdapr-sdkでconflict。 現在利用しているquarkusのmoduleは利用していないので、dapr-sdkの依存バージョンでoverride -->
      <dependency>
         <groupId>com.squareup.okhttp3</groupId>
         <artifactId>okhttp</artifactId>
         <version>${okhttp.version}</version>
      </dependency>
      <dependency>
        <groupId>com.squareup.okio</groupId>
        <artifactId>okio</artifactId>
        <version>${okio.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.stock.groupId}</groupId>
        <artifactId>${nautible.app.stock.name}-core</artifactId>
        <version>${nautible.app.stock.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.stock.groupId}</groupId>
        <artifactId>${nautible.app.stock.name}-aws</artifactId>
        <version>${nautible.app.stock.version}</version>
      </dependency>
      <dependency>
        <groupId>${nautible.app.stock.groupId}</groupId>
        <artifactId>${nautible.app.stock.name}-azure</artifactId>
        <version>${nautible.app.stock.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.jboss.jandex</groupId>
          <artifactId>jandex-maven-plugin</artifactId>
          <version>${jandex.maven.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-maven-plugin</artifactId>
          <version>${quarkus-plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler-plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.openapitools</groupId>
          <artifactId>openapi-generator-maven-plugin</artifactId>
          <version>${openapi.generator.maven.plugin.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${surefire-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <profiles>
    <!-- 
      最新のmvn(3.8.5)を利用した場合、quarkus-maven-pluginではコマンドライン引数(-P xxx)でのプロファイル指定が有効とならない。
      system propertyでのプロファイルの有効化が機能するのでsystem propertyを利用する。
    -->
    <profile>
      <id>default</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <modules>
        <module>nautible-app-ms-stock-core</module>
        <module>nautible-app-ms-stock-aws</module>
        <module>nautible-app-ms-stock-azure</module>
        <module>nautible-app-ms-stock-build</module>
      </modules>
    </profile>
    <profile>
      <id>aws</id>
      <activation>
        <property>
          <name>cloud</name>
          <value>aws</value>
        </property>
      </activation>
      <modules>
        <module>nautible-app-ms-stock-core</module>
        <module>nautible-app-ms-stock-aws</module>
        <module>nautible-app-ms-stock-build</module>
      </modules>
    </profile>
    <profile>
      <id>azure</id>
      <activation>
        <property>
          <name>cloud</name>
          <value>azure</value>
        </property>
      </activation>
      <modules>
        <module>nautible-app-ms-stock-core</module>
        <module>nautible-app-ms-stock-azure</module>
        <module>nautible-app-ms-stock-build</module>
      </modules>
    </profile>
  </profiles>
</project>
